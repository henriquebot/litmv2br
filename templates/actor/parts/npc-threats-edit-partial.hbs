<fieldset>
    <legend>{{localize 'MIST_ENGINE.LABELS.ThreatsAndConsequences'}} <button data-action="createThreatAndConsequence"><i class='fas fa-plus'></i></button>
    </legend>
    {{!-- <div class="form-group"> --}}
        {{#each system.threatsAndConsequences as |entry index|}}
        <div class="threat-and-consequence-container-entry box-shadowed">
            <p class="help-text">{{localize 'MIST_ENGINE.HELP.GMTagShortCuts'}}</p>
            <div class="form-group flexcol">
                <div class="form-group flexrow" style="margin-bottom: 5px;">
                    <input type="text" class="npc-updatable-npc-array-stat" data-index="{{index}}"
                        data-array="threatsAndConsequences" data-key="name" id="limit-{{index}}"
                        name="system.threatsAndConsequences[{{index}}].name" value="{{entry.name}}"
                        placeholder="{{localize 'MIST_ENGINE.PLACEHOLDERS.Name'}}" />
                    <div class="trash-icon-container">
                        <a class="" data-action="deleteThreatAndConsequence" data-index="{{index}}">
                            <i class='fas fa-trash'></i>
                        </a>
                    </div>
                </div>
                <textarea class="npc-updatable-npc-array-stat" data-index="{{index}}"
                    data-array="threatsAndConsequences" data-key="description" id="limit-{{index}}"
                    name="system.threatsAndConsequences[{{index}}].description"
                    placeholder="{{localize 'MIST_ENGINE.PLACEHOLDERS.Description'}}">{{entry.description}}</textarea>
            </div>
            <fieldset>
                <legend>{{localize 'MIST_ENGINE.LABELS.Consequences'}} <button data-action="createThreatAndConsequenceEntry" data-index="{{index}}"><i class='fas fa-plus'></i></button></legend>
                {{#each entry.list as |listEntry listIndex|}}
                <div class="threat-entry-container form-group flexrow">
                    <textarea class="npc-updatable-threat-entry-stat value-input-huge"
                        id="npc-thread-list-{{index}}-{{listIndex}}" data-index="{{index}}"
                        data-listIndex="{{listIndex}}" placeholder="">{{listEntry}}</textarea>
                    <div class="trash-icon-container">
                        <a class="" data-action="deleteThreatAndConsequenceEntry" data-index="{{index}}"
                            data-listIndex="{{listIndex}}">
                            <i class='fas fa-trash'></i>
                        </a>
                    </div>
                </div>
                {{/each}}
                <button data-action="createThreatAndConsequenceEntry" data-index="{{index}}"><i class='fas fa-plus'></i>{{localize 'MIST_ENGINE.LABELS.Consequences'}}</button>
            </fieldset>
        </div>
        <hr />
        {{/each}}
        {{!--
    </div> --}}
    <button data-action="createThreatAndConsequence"><i class='fas fa-plus'></i> {{localize 'MIST_ENGINE.LABELS.ThreatsAndConsequences'}}</button>
</fieldset>